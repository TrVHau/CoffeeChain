# Network environment template for Azure Linux VM deployment

# On Linux VM, keep runtime files under project directory
CRYPTO_BASE=./crypto-config
ARTIFACTS_BASE=./channel-artifacts

# Used by backend and frontend services in docker-compose.yaml
TRACE_PUBLIC_BASE_URL=https://app.your-domain.com/trace/
NEXT_PUBLIC_API_URL=https://api.your-domain.com

# Replace with strong secret (32+ chars)
JWT_SECRET=<replace-with-strong-32-plus-char-secret>
